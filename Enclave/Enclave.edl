enclave
{
	trusted
	{
		/*These are ECALL defines.*/
		public int ecall_generate_keys([in]const unsigned char *data);
	};

	untrusted
	{
		/*These are OCALL defines.*/
        void ocall_enc_data([in]unsigned char *penc_data, [in]size_t *size);
        void ocall_dec_data([in]unsigned char *pdec_data, [in]size_t *size);
	};
};
